<% if logged_in? %>
    <h1>Hello <%= current_user.username %> !</h1>
    <%= button_to "Logout", '/logout', method: :delete%>
<%end%>

<h1>Tweets</h1>
<ul>
  <% @tweets.each do |tweet| %>
    <li>
      <%= tweet.content %>
    </li>
  <% end %>
</ul>
